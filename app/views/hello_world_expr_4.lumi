<pre>
{{ php_sapi_name() }}

{{ php_uname('v') }}

{{ printf('%d %d %s', 1, 2, 3) }}

{{ printf('%s %s %d', 'a', 'b', 'c',) }}

{{ logger.all() }}

{{ printf('%d %d', (1 + 2), 3 ) }}

{# set entry = logger.entries[0] #}
{% set entry = first(logger.entries) %}

{{ printf('%s %s', entry.date, entry.message) }}
</pre>